import { Observable } from 'rxjs';
import { XHROptions, XHRResponse } from './xhr.solution';

export function xhr(method: string, url: string, options?: XHROptions): Observable<XHRResponse> {
  // TODO: implement!
}
